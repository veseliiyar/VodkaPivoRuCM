This directory intentionally contains silent placeholder assets.

Real assets are maintained in the private ColonialMarinesAudio repository.

Currently supports .ogg and .mp3 files.

During packaging and publishing, these placeholders are overwritten with real files.

After adding a track to CMAudio, run ./Tools/sync_audio_placeholders.py for the public repo to use.
