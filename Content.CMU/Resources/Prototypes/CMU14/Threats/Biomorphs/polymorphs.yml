# Mimic chooses a profile -> polymorphs into a real MobHuman. The server-side
# AbominationMimicSystem then patches that human's appearance / factions /
# skills onto it from the snapshot.
- type: polymorph
  id: BiomorphMimicDisguise
  configuration:
    entity: CMMobHuman
    forced: true
    inventory: Drop
    transferName: false
    transferDamage: true
    # Converted mimics already have a polymorph that disallows further morphs.
    # The disguise must bypass that existing form's restriction.
    ignoreAllowRepeatedMorphs: true
    allowRepeatedMorphs: true
    # We handle crit/death/expiry ourselves so we can run the shake-and-scream
    # revert sequence; the polymorph layer must not auto-revert.
    revertOnCrit: false
    revertOnDeath: false

# Assimilation completes -> the victim is polymorphed into an abomination mimic.
# One-way: they don't auto-revert when they die or get crit, they just stay a mimic.
- type: polymorph
  id: BiomorphAssimilationToMimic
  configuration:
    entity: AU14BiomorphMimic
    forced: true
    inventory: Drop
    transferName: false
    transferDamage: false
    revertOnCrit: false
    revertOnDeath: false

# Non-humanoid (animal) assimilation/turn-on-death — they become a spider
# instead of a humanoid mimic. Same one-way semantics.
- type: polymorph
  id: BiomorphAssimilationToSpider
  configuration:
    entity: AU14BiomorphSpider
    forced: true
    inventory: Drop
    transferName: false
    transferDamage: false
    revertOnCrit: false
    revertOnDeath: false

# Infected death sometimes spawns a skitter instead of a mimic — see the
# 50/50 roll in AbominationInfectionSystem.
- type: polymorph
  id: BiomorphAssimilationToSkitter
  configuration:
    entity: AU14BiomorphSkitter
    forced: true
    inventory: Drop
    transferName: false
    transferDamage: false
    revertOnCrit: false
    revertOnDeath: false
